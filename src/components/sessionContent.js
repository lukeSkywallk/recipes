import React from 'react'

function SessionContent({sessionContent}){

    return(
        <React.Fragment>
            {sessionContent}
            <br/>
        </React.Fragment>
    )
}

export default SessionContent;